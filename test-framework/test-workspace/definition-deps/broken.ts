/**
 * ============================================================================
 * Файл с намеренными синтаксическими ошибками для негативных тестов
 * ============================================================================
 * 
 * Этот файл содержит намеренные синтаксические ошибки.
 * Используется для проверки поведения LSP при работе с некорректным кодом.
 * 
 * ВНИМАНИЕ: Этот файл НЕ КОМПИЛИРУЕТСЯ - это сделано намеренно!
 */

/**
 * Синтаксическая ошибка: отсутствует закрывающая скобка
 * Symbol: brokenFunction
 * Command: textDocument/definition (из другого файла)
 * Expected: ОШИБКА - файл не может быть разобран из-за синтаксической ошибки
 */
export function brokenFunction(: string {  // Ошибка: пропущен параметр
    return "This function has a syntax error";
}

/**
 * Попытка экспорта после синтаксической ошибки
 * Symbol: unreachableExport
 * Command: textDocument/definition
 * Expected: ОШИБКА - код после синтаксической ошибки недоступен
 */
export const unreachableExport = "This may not be reachable";

// Примечание: Из-за синтаксической ошибки выше, весь файл может быть
// недоступен для LSP-анализа
